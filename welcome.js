(()=>{const screen=document.getElementById('welcome-screen');if(!screen)return;let timeout=null,locked=[],priorFocus=null;function unlock(){for(const [el,inert] of locked)el.inert=inert;locked=[];document.body.classList.remove('welcome-open');}function hide(){screen.hidden=true;unlock();if(priorFocus?.isConnected)priorFocus.focus?.({preventScroll:true});}function show(){clearTimeout(timeout);unlock();priorFocus=document.activeElement;screen.hidden=false;screen.classList.remove('welcome-play');void screen.offsetWidth;screen.classList.add('welcome-play');document.body.classList.add('welcome-open');locked=[...document.body.children].filter(el=>el!==screen&&!['SCRIPT','STYLE'].includes(el.tagName)).map(el=>[el,el.inert]);for(const [el] of locked)el.inert=true;timeout=setTimeout(hide,3000);}show();document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')show();else{clearTimeout(timeout);hide();}});window.addEventListener('pageshow',e=>{if(e.persisted)show();});})();
